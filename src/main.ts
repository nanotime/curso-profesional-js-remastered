/**
 * En este archivo es donde importaremos todo el codigo principal que necesitemos para lanzar nuestro
 * proyecto, aquí por el momento solo se buildea la página de inicio como indice del proyecto.
 */

import { setPages } from './setup';
setPages();